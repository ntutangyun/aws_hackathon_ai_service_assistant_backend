# Environment Configuration
# Set to "local" for local development (uses localhost MCP servers)
# Set to "production" for AWS deployment (uses AgentCore Runtime)
ENVIRONMENT=local

# AWS Configuration
AWS_REGION=us-east-1

# Bedrock AgentCore Configuration
# Agent name is used to construct paths for SSM/Secrets Manager
# The service will fetch everything dynamically per request:
# - Agent ARN from: /agent/{AGENT_NAME}/runtime/agent_arn (SSM)
# - Cognito credentials from: /agent/{AGENT_NAME}/cognito/credentials (Secrets Manager)
AGENT_NAME=ai_assistant_agent

# Request timeout (seconds)
REQUEST_TIMEOUT_SECONDS=300

# API Configuration
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:3000
API_PORT=8000

# Local MCP Server Ports (for local development only)
LOCAL_UDM_PORT=9003
LOCAL_EDGE_SERVER_PORT=9001
LOCAL_AI_SERVICE_PORT=9002

# Note: All authentication is fetched dynamically from AWS on each request.
# No credentials are cached or stored in environment variables.
